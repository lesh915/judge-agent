"""Optional backend API placeholder.

FastAPI is intentionally not required for the MVP test agent. The CLI is the
primary interface. This module documents where a future FastAPI app can expose
/v1/analyze and fixture endpoints described in test/agent/BACKEND_API.md.
"""
